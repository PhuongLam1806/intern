const svg = {
    slider1: require('./slider1.svg').default,
    email: require('./Email.svg').default,
    password: require('./password.svg').default,
    logo: require('./logo.svg').default,
};

export default svg;
